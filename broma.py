mkdir broma-api
cd broma-api

# Сохрани main.py, requirements.txt и .gitignore в эту папку

git init
git add .
git commit -m "Initial commit of Broma API"

# Создай репозиторий на GitHub через веб-интерфейс (например, broma-api)

# После создания репозитория GitHub покажет команды:

git remote add origin https://github.com/SifArtz/broma-api.git
git branch -M main
git push -u origin main
